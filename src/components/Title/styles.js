import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  textTitle: {
    color: "orange",
    fontSize: 30,
    alignSelf: "center",
    marginBottom: 70
  }
});

export default styles;